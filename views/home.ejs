<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Home Page</title>
</head>

<body>
    <h1> URL SHORTENER</h1>

    <% if (locals.id) { %>
        <h1> URL Generated - http://localhost:8001/url/<%= id %>
        </h1>
        <% } %>

            <form method="POST" action="/url">
                <label>Enter your url</label>
                <input type="text" name="url" placeholder="www.example.com" />
                <button type="" submit">Generate</button>
            </form>

            <div>
                <% if (locals.urls) { %>
                    <table>
                        <thead>
                            <th>S. No.</th>
                            <th>Shortid</th>
                            <th>Redirect URL</th>
                            <th>Clicks</th>
                        </thead>
                        <% urls.forEach((url, index)=> { %>
                            <tr>
                                <td>
                                    <%= index+1 %>
                                </td>
                                <td>
                                    <%= url.shortId %>
                                </td>
                                <td>
                                    <%= url.redirectURL %>
                                </td>
                                <td>
                                    <%= url.visitHistory.length %>
                                </td>
                                <td>
                                    <a href=http://localhost:8001/url/<%=url.shortId %> target="_blank">Link</a>
                                </td>

                            </tr>
                            <% }) %>
                    </table>
                    <% } %>
            </div>

</body>

</html>